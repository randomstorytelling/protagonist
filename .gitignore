# personal / generated data
save.json
save.json.bak
*.tmp
# NOTE: whoop-today.json IS published (the live WHOOP feed the app auto-loads). It holds
# recovery/sleep/strain/workouts — biometrics on a public URL. Tradeoff accepted for zero-tap sync.

# secrets — Firebase Admin service-account keys (NEVER commit; live in ~/.protagonist/)
*serviceAccount*.json
*-firebase-adminsdk-*.json
sa.json

# local-only helpers
deploy/
serve.js

# misc
node_modules/
.DS_Store
