# personal / generated data — NEVER publish (biometrics)
save.json
save.json.bak
*.tmp
# whoop-today.json holds recovery/sleep/strain — stays PRIVATE. The live feed now writes it
# straight to per-user Firestore (whoop-feed.js), so it must never sit on a public URL.
whoop-today.json

# secrets — Firebase Admin service-account keys (NEVER commit; live in ~/.protagonist/)
*serviceAccount*.json
*-firebase-adminsdk-*.json
sa.json

# local-only helpers
deploy/
serve.js

# misc
node_modules/
.DS_Store
