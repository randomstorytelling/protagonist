# personal / generated data — never publish
save.json
save.json.bak
whoop-today.json
*.tmp

# secrets — Firebase Admin service-account keys (NEVER commit; live in ~/.protagonist/)
*serviceAccount*.json
*-firebase-adminsdk-*.json
sa.json

# local-only helpers
deploy/
serve.js

# misc
node_modules/
.DS_Store
